import React, { Fragment } from "react";
import Layout from "@/components/layout/layout";

const DashBoard = () => {
  return (
    <Fragment>
      <Layout />
    </Fragment>
  );
};

export default DashBoard;
